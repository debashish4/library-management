Code Repository for Library Management for Education
========================================================


### Install

* Download/clone the repo
* Run `npm install`
* Run `gulp` and navigate to `http://localhost:8000` to view the pages

### Tutorial